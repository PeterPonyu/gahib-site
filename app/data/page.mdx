import { FigureCard } from '@/components/FigureCard';
import { EmbargoNote, NextSteps, PageBody, PageHeader, SectionHeader } from '@/components/PageScaffold';
import { DatasetInventory, PreprocessingList, StudyScope } from '@/components/StudyInventory';

<PageHeader
  eyebrow="Evaluation data"
  title="Datasets"
  summary={
    <>
      GAHIB is evaluated on 53 single-cell RNA-seq datasets spanning cancer and development. The inventory mirrors the manuscript taxonomy and uses the same selected dataset identifiers as the experiment runners.
    </>
  }
>
  <EmbargoNote>
    Public metadata are shown here; raw counts, cell barcodes, model weights, and unpublished result matrices remain outside the site.
  </EmbargoNote>
</PageHeader>

<PageBody>
  <StudyScope />

  <SectionHeader
    eyebrow="Inventory"
    title="Dataset identifiers"
    description="Cancer and development cohorts are grouped into the categories used throughout the manuscript and experiment runners."
  />

  <DatasetInventory />

  <PreprocessingList />

  <SectionHeader
    eyebrow="Reference figure"
    title="Manuscript taxonomy figure"
    description="Kept below the metadata inventory for site readers; the standalone site-overview figure does not repeat manuscript figures."
  />

  <FigureCard
    src="/gahib-site/figures/dataset_taxonomy.png"
    alt="Taxonomy of the 53 single-cell datasets used to evaluate GAHIB"
    caption="Dataset taxonomy across cancer and developmental contexts. Counts and identifiers match the manuscript evaluation cohort."
    width={1600}
    height={1000}
  />

  <section className="my-8 max-w-3xl">
    <h2 className="mb-3 text-2xl font-semibold text-slate-900">Acquisition</h2>
    <div className="leading-relaxed text-slate-700">
      Datasets are obtained from their original publications or public archive
      records. Public examples such as <code>paul15</code> and
      <code className="ml-1">pbmc3k_processed</code> are available through
      standard Scanpy loaders; larger benchmark cohorts are resolved by the
      experiment runners from local dataset directories.
    </div>
  </section>

  <section className="my-8 max-w-3xl rounded-lg border border-slate-200 bg-slate-50 p-5">
    <h2 className="mb-3 text-2xl font-semibold text-slate-900">Confidentiality note</h2>
    <div className="leading-relaxed text-slate-700">
      This site does not host raw data, cell barcodes, sample identifiers,
      model weights, or unpublished result matrices. It documents the metadata
      needed to understand the evaluation design.
    </div>
  </section>

  <NextSteps
    items={[
      { href: '/metrics/', label: 'Next', title: 'Metrics', description: 'Inspect the 20 metric definitions used to evaluate every latent representation.' },
      { href: '/methods/', label: 'Then', title: 'Benchmarks', description: 'Review the benchmark families and method tracks compared against GAHIB.' },
      { href: '/method/', label: 'Back', title: 'Method', description: 'Return to the model architecture and objective details.' },
    ]}
  />
</PageBody>
